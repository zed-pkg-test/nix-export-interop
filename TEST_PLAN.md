# Test plan

- Verify deterministic Zed-to-Nix export, immutable hashes, and repeatable offline builds across the supported happy-path states and canonical fixtures.
- Verify deterministic Zed-to-Nix export, immutable hashes, and repeatable offline builds under retries, interruption, concurrency, offline operation, or partial failure.
- Verify deterministic Zed-to-Nix export, immutable hashes, and repeatable offline builds preserves authorization, idempotency, integrity, observability, and actionable failure classification.

## Classification

- product regression
- blocked dependency
- harness regression
